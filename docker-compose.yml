version: '3'
services:
  messaging:
    image: 'rabbitmq:3-management'
    ports:
      - 5672:5672
      - 15672:15672
  postgres:
    image: 'postgres'
    environment:
      POSTGRES_PASSWORD: changeme
  front_end:
    build: ./frontend
    ports:
        - 5000:5000
